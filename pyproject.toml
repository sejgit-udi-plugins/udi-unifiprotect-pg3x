[project]
name = "udi-unifiprotect-pg3x"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
    "udi-interface>=3.3.12",
    "aiohttp",
]

[dependency-groups]
dev = [
    "pre-commit",
    "pytest",
    "pytest-cov",
]
lint = [
    "ruff",
]

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
